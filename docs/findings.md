# Trois observations chiffrées tirées des données

> Stub — à compléter dans l'issue **I6.7** (WS-platform), à partir des données **réellement
> importées** (pas de valeurs générées).

Pour **chaque** observation (3 minimum) :

- L'énoncé chiffré (ex. « sur TraceLab, N % des sessions utilisent l'outil X »).
- Source(s) et version du dataset.
- Filtres exacts (source, agent, modèle, période) permettant de retrouver le chiffre dans le
  dashboard.
- Requête ou capture correspondante.

Mentionner aussi : outils IA employés pendant le développement, composants externes réutilisés.
